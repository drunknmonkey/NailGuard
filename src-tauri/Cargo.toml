[package]
name = "nailguard-desktop"
version = "0.1.0"
description = "NailGuard Desktop (Tauri-Spike)"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
